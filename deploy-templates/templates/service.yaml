apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-gradle-java25-app.fullname" . }}
  labels:
    {{- include "java-gradle-java25-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-gradle-java25-app.selectorLabels" . | nindent 4 }}
