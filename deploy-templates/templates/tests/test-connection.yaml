apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "j25-grd-cln-gh-smvr-bld.fullname" . }}-test-connection"
  labels:
    {{- include "j25-grd-cln-gh-smvr-bld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "j25-grd-cln-gh-smvr-bld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
